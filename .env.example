# 이 프로젝트 전용 환경 변수. .env로 복사한 뒤 값만 채운다.
# 다른 Playwright 프로젝트와 격리하려면 PLAYWRIGHT_BROWSERS_PATH는 여기 넣지 말 것.
# (브라우저 경로는 package.json 스크립트에서만 .playwright-browsers 로 지정됨)

# ZeroStep AI (로케이터 실패 시 같은 런 안에서 액션 재시도용). Chromium에서만 동작.
ZEROSTEP_TOKEN=

# 로그인 시나리오용 (선택). 없으면 해당 시나리오에서 직접 입력 필요.
# 성인 인증 계정 (연령 제한 콘텐츠/인증 후 UI 검증용)
# AUTH_ADULT_USERNAME=
# AUTH_ADULT_PASSWORD=
# 성인 미인증 계정 (미인증 시 안내/제한 화면 검증용).
# 기존 AUTH_USERNAME/AUTH_PASSWORD가 있으면 미인증 계정으로 대신 사용됨.
# AUTH_NON_ADULT_USERNAME=
# AUTH_NON_ADULT_PASSWORD=

# ZeroStep AI 비활성화 시 "true". 기본은 비어 있거나 "false".
# ENABLE_ZEROSTEP_AI=false

# 테스트 대상 베이스 URL. 접속/로그인 리다이렉트/권한 origin 등에서 사용.
BASE_URL=https://page.kakao.com/

# OpenAI GPT-4o API 토큰. (현재 코드에서 미사용. 로케이터 실패 시 AI 재시도는 ZEROSTEP_TOKEN 사용.)
# OPENAI_API_KEY=

# 슬랙 알림 (선택). 설정 시 테스트 후 결과 요약을 해당 웹훅으로 전송. 비어 있으면 전송하지 않음.
# SLACK_WEBHOOK_URL=
