# 이 프로젝트 전용 환경 변수. .env로 복사한 뒤 값만 채운다.
# 다른 Playwright 프로젝트와 격리하려면 PLAYWRIGHT_BROWSERS_PATH는 여기 넣지 말 것.
# (브라우저 경로는 package.json 스크립트에서만 .playwright-browsers 로 지정됨)

# ZeroStep AI (로케이터 실패 시 같은 런 안에서 액션 재시도용). Chromium에서만 동작.
ZEROSTEP_TOKEN=

# 로그인 시나리오용 (선택). 없으면 해당 시나리오에서 직접 입력 필요.
# AUTH_USERNAME=
# AUTH_PASSWORD=

# ZeroStep AI 비활성화 시 "true". 기본은 비어 있거나 "false".
# ENABLE_ZEROSTEP_AI=false
